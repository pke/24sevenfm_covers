# This repository also contains native Windows projects, SDKs, installers and the
# separately deployed website. The Vercel project needs only its functions and
# Node dependency manifests.
/*
!api
!package.json
!package-lock.json
!vercel.json
!vercel-ignore-build.cjs

# Files under api/ become function entry points, including unit tests.
api/**/*.test.js
